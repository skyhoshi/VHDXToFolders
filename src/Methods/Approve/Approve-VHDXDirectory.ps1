function Approve-VHDXDirectory(){
    <#
    .SYNOPSIS
        Validates and returns Enum (integer) that indicates the state of the directory provided.
    .DESCRIPTION
        Long description
    .EXAMPLE
        PS C:\> Approve-VHDXDirectory -Path C:\
        Explanation of what the example does
    .INPUTS
        Inputs (if any)
    .OUTPUTS
        Output (if any)
    .NOTES
        General notes
    #>
    
    }